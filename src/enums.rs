// Types of film
pub enum FilmType { /*CINEMA = 0,*/ TELEVISION = 1 }

// Types of support (bitfield, for filters)
pub enum SupportType {
    //TAPE = 1,
    //DVD = 2,
    COMPUTERFILE = 4,
    //BLURAY = 8,
    //ALL = 15
}
